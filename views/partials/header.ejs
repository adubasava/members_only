<header>
  <nav
    class="flex items-center justify-between bg-primary-blackish p-4 text-white"
  >
    <a href="/">Welcome to the Club</a>
    <div class="flex gap-3 text-sm">           
      <% if (user) {%>
        <a href="/messages/new">Create New Message</a>
        <% if (!user.status) {%>
          <a href="/users/membership">Gain Membership</a>  
        <% }%>               
        <a href="/log-out">LOG OUT</a>
        <% } else { %>
          <a href="/sign-up">Sign Up</a> 
          <a href="/log-in">Log In</a> 
        <%}%>
    </div>
  </nav>
</header>
